interface UI {
    isOpen: boolean,
    accordionMode: number,
    columnMode: string,
    uiMode: boolean,
    alertMode: boolean,
    showTableColumn: boolean,
    hasAccessRequest: boolean,
};

export default UI;